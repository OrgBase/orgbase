<%= react_component('Room', {
    roomName: @room.slug,
    token: @token,
    roomSid: @twilio_room.sid,
    roomShared: {
        panelType: @room.panel_type || @starting_game.game_type,
        panelId: @room.panel_id || @starting_game.id,
        randomFraction: @room.random_fraction,
    }
}) %>