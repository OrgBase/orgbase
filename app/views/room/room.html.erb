<%= react_component('Room', {
    roomName: @room.slug,
    token: @token,
    roomSid: @twilio_room.sid,
    roomShared: {
        panelType: @room.panel_type,
        panelId: @room.panel_id
    }
}) %>