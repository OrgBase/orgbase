<%= react_component('Session', {
    sessionSlug: @session.slug,
    config: {
        scheduledAt: @session.config.scheduled_at.to_i,
        cutOffSeconds: @session.config.cut_off_seconds.seconds,
    }
}) %>